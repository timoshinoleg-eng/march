export const metadata = {
  title: "Kimi Agent 2.5 - Admin Dashboard",
  description: "Analytics Dashboard for AI Chat Widget",
};

export default function AdminLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
